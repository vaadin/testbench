name: TestBench Validation

on:
  push:
    branches:
      - '5.4'
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

env:
  JAVA_VERSION: 8

jobs:
  build-and-test:
    name: Build and Test (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 45

    strategy:
      fail-fast: false
      matrix:
        browser: [ chrome, firefox ]

    services:
      selenium:
        image: selenium/standalone-${{ matrix.browser }}:latest
        env:
          SCREEN_WIDTH: 1650
          SCREEN_HEIGHT: 1250
        ports: ['4444:4444', '7900:7900']
        options: --shm-size=2g
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: "temurin"

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Clean Maven cache (if needed)
        if: github.event_name == 'workflow_dispatch'
        run: |
          echo "---> Clean maven cache"
          rm -rf ~/.m2/repository ~/.npm* ~/.pnpm*

      - name: Set PRO License
        run: |
          TB_LICENSE=${{secrets.TB_LICENSE}}
          mkdir -p ~/.vaadin/
          echo '{"username":"'`echo $TB_LICENSE | cut -d / -f1`'","proKey":"'`echo $TB_LICENSE | cut -d / -f2`'"}' > ~/.vaadin/proKey

      - name: Build with Maven
        run: |
          mvn clean install -DskipTests -B

      - name: Run Tests and Generate Javadoc
        run: |
          mvn javadoc:javadoc verify \
            -P validation \
            -Dbrowsers.include=${{ matrix.browser }} \
            -Dvaadin.testbench.developer.license=${{ secrets.TB8_LICENSE }} \
            -Dtestbench.javadocs \
            -Dsystem.com.vaadin.testbench.Parameters.testsInParallel=5 \
            -Dsystem.com.vaadin.testbench.Parameters.maxAttempts=2 \
            -Dcom.vaadin.testbench.Parameters.hubHostname=localhost \
            -Dfailsafe.forkCount=5 \
            -Dsurefire.printSummary=true \
            -Dsurefire.reportFormat=plain \
            -B \
            -fae

      - name: Upload error screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: error-screenshots-${{ github.run_id }}-${{ matrix.browser }}
          path: |
            **/error-screenshots/**
          retention-days: 7

      - name: Upload test reports on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports-${{ github.run_id }}-${{ matrix.browser }}
          path: |
            **/target/surefire-reports/
            **/target/failsafe-reports/
          retention-days: 7

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ github.run_id }}-${{ matrix.browser }}
          path: |
            **/target/surefire-reports/TEST-*.xml
            **/target/failsafe-reports/TEST-*.xml
          retention-days: 7

      - name: Publish test results
        if: always()
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: |
            **/target/surefire-reports/TEST-*.xml
            **/target/failsafe-reports/TEST-*.xml
          check_name: Test Results
          comment_mode: failures

      # 🆕 New step: Append skipped test names to GHA summary
      - name: Summarize skipped tests (${{ matrix.browser }})
        if: always()
        run: |
          echo "### ⚠️ Skipped Tests (${{ matrix.browser }})" >> $GITHUB_STEP_SUMMARY
          SKIPPED=$(grep -R "<skipped" **/target/*-reports/TEST-*.xml || true)
          if [ -z "$SKIPPED" ]; then
            echo "No skipped tests found." >> $GITHUB_STEP_SUMMARY
          else
            echo "$SKIPPED" | \
              sed -E 's|.*/TEST-(.*)\.xml:.*name="([^"]*)".*|• \1 :: \2|' | \
              sort -u >> $GITHUB_STEP_SUMMARY
            COUNT=$(echo "$SKIPPED" | wc -l)
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Total skipped tests:** $COUNT" >> $GITHUB_STEP_SUMMARY
          fi
