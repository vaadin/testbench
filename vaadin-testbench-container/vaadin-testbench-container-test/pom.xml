<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <artifactId>vaadin-testbench-container</artifactId>
    <groupId>com.vaadin</groupId>
    <version>7.0-SNAPSHOT</version>
  </parent>

  <artifactId>vaadin-testbench-container-test</artifactId>
  <packaging>pom</packaging>

  <modules>
    <module>vaadin-testbench-container-test-meecrowave</module>
    <module>vaadin-testbench-container-test-springboot-v01</module>
    <module>vaadin-testbench-container-test-springboot-v02</module>
    <module>vaadin-testbench-container-test-none</module>
  </modules>

  <properties>
    <maven.deploy.skip>true</maven.deploy.skip>
  </properties>

  <!-- Just to disable any accidential deployment to maven.central -->
  <distributionManagement>
    <repository>
      <id>localhost</id>
      <url>file://${basedir}/target/repo/</url>
    </repository>
    <snapshotRepository>
      <id>localhost</id>
      <url>file://${basedir}/target/snapshot-repo/</url>
    </snapshotRepository>
  </distributionManagement>

  <profiles>
    <profile>
      <id>update-license-headers</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>license-maven-plugin</artifactId>
            <configuration>
              <skipUpdateLicense>true</skipUpdateLicense>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
